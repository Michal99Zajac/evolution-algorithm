from typing import List

from models.chromosome import BinaryChromosome

class BinaryChromosomeAdapter(BinaryChromosome):
    def __init__(self, gens: List[bool]):
        super().__init__(gens)
        self.__value =  a + decimal(łańcuch_binarny) * (b-a)/(2**m  - 1)
